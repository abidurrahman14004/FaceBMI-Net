# Streamlit (for Streamlit Cloud deployment)
streamlit>=1.28.0

# Image processing
Pillow>=10.0.0
# OpenCV is NOT required - we use PIL for all image conversions
# If you need OpenCV for other purposes, install: opencv-python-headless>=4.8.0

# Data processing
# numpy version constraint: TensorFlow 2.19.0 requires numpy<2.2.0,>=1.26.0
numpy>=1.26.0,<2.2.0
pandas>=2.0.0
scipy>=1.11.0

# Machine learning - flexible versions for automatic resolution
torch>=2.0.0
torchvision>=0.15.0

# Scikit-learn
scikit-learn>=1.3.0

# MediaPipe for facial landmark extraction
# Version >=0.10.30 required for Python 3.13 compatibility on Streamlit Cloud
# Available versions for Python 3.13: 0.10.30, 0.10.31
# This version constraint works on both Python 3.12 (local) and Python 3.13 (Streamlit Cloud)
mediapipe>=0.10.30

# Utilities
requests>=2.31.0
